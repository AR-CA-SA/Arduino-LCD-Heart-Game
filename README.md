# Arduino 4 Button Heart Mini Game
The following code executes a process designed for the Arduino UNO R3. This process takes depends on t 4 digital inputs (buttons) to move the character and avoid the bullets.

